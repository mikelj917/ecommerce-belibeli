import { StorePage } from "@/app/(view)/(store)/_store/StorePage";

const Home = async () => {
  return (
    <div className="relative z-10 overflow-x-hidden">
      <StorePage />
    </div>
  );
};

export default Home;
