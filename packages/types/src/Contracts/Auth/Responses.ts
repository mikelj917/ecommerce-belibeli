export type LoginResponse = {
  user: {
    id: number;
    name: string;
    email: string;
  };
};

export type RegisterResponse = {
  user: {
    id: number;
    name: string;
    email: string;
  };
};
